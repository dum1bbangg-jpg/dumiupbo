/* Replace the two values below with your own Supabase project settings.
   Supabase dashboard > Project Settings > Data API
     SUPABASE_URL      = Project URL
     SUPABASE_ANON_KEY = anon public key
   The anon key is meant to be public. Write access is granted by the
   row level security policies in supabase.sql, not by hiding this key. */
window.DOOMI_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: "",

  /* Optional. 두미의 SOOP 아이디를 넣으면 사이드바 프로필 사진을 SOOP에서
     원본 해상도로 가져옵니다. 비워 두면 assets/doomi-profile.png를 씁니다. */
  SOOP_ID: ""
};
